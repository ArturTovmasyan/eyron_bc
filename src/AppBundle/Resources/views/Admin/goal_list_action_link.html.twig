{% if admin.isGranted('VIEW', object) and admin.hasRoute('show') %}

    {% if not object.archived %}
        <a href="{{ path('inner_goal', {'slug': object.slug}) }}" target="_blank" class="btn btn-sm btn-default view_link" title="{{ 'admin.label.name.goal_link'|trans() }}">
            <i class="glyphicon glyphicon-home"></i>
        </a>
    {% endif %}
{% endif %}
