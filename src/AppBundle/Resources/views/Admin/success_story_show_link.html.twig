{% extends 'SonataAdminBundle:CRUD:base_show_field.html.twig' %}

{% block field %}
    {{ value }}
    <a class="btn btn-sm btn-default view_link" href="{{ path('inner_goal', {'slug': object.goal.slug }) }}">{{ object.goal.title }}</a>
{% endblock %}